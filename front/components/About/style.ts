import { makeStyles } from "@material-ui/styles";


export const aboutStyles = makeStyles({
    box: {
        minWidth: "100vh", 
        minHeight: "100vh",
        width: "100%",
        height: "100%",
        marginTop: "7.5%"
    }
});